# 测试 / CI 环境：禁用交互式清空重建（system.bootstrap.interactive-rebuild）。
# SystemBootstrapService 退回"安全模式"——仅建缺失表或校验现有表，不 drop、不读 stdin、不交互，
# 避免测试因无 tty 卡死或误清库。需测试重建逻辑时在具体测试类用 @TestPropertySource 覆盖。
system.bootstrap.interactive-rebuild=false
